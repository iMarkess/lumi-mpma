{
  "name": "sentinela-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API de denúncias do LUMI MPMA (Fastify + Prisma + PostgreSQL).",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "prisma db push --skip-generate && node src/server.js",
    "seed": "node prisma/seed.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^9.0.1",
    "@fastify/rate-limit": "^10.1.1",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "fastify": "^5.1.0",
    "google-auth-library": "^9.15.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
